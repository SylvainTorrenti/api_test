/User:
  post:
    x-swagger-router-controller: User
    description: Stock in local storage
    operationId: setUser
    tags:
      - Create user
    parameters:
      - in: body
        name: user
        description: The user to create.
        schema:
          type: object
          required:
            - nom
          properties:
            nom:
              type: string
            prenom:
              type: string
    responses:
      201:
        description: Created

  get:
    x-swagger-router-controller: User
    description: read data from local file storage
    operationId: getUser
    tags:
      - Get user
    parameters:
    responses:
      "200":
        description: Success
        schema:
          $ref: "#/definitions/ObjectResponse"
      default:
        description: Error
        schema:
          $ref: "#/definitions/ErrorResponse"

  put:
    x-swagger-router-controller: User
    description: change data in local file storage
    operationId: changeUser
    parameters:
      - name: data
        in: body
        description: data to change in storage
        required: true
        schema:
          $ref: "#/definitions/ObjectResponse"
    tags:
      - Change user
    responses:
      "200":
        description: Success
        schema:
          $ref: "#/definitions/ObjectResponse"
      default:
        description: Error
        schema:
          $ref: "#/definitions/ErrorResponse"

  delete:
    x-swagger-router-controller: User
    description: delete data from local file storage
    operationId: deleteUser
    tags:
      - Delete user
    responses:
      "200":
        description: Success
        schema:
          $ref: "#/definitions/ObjectResponse"
      default:
        description: Error
        schema:
          $ref: "#/definitions/ErrorResponse"
/User/{id}:
  get:
    x-swagger-router-controller: User
    description: read data from local file storage by id
    operationId: getUserById
    tags:
      - Get user
    parameters:
      - name: id
        in: path
        description: ID of the storage object to retrieve
        required: true
        type: string
    responses:
      "200":
        description: Success
        schema:
          $ref: "#/definitions/ObjectResponse"
      default:
        description: Error
        schema:
          $ref: "#/definitions/ErrorResponse"

  put:
    x-swagger-router-controller: User
    description: change data of a specific data in local file storage
    operationId: changeUserById
    parameters:
      - name: id
        in: path
        description: ID of the storage object to retrieve
        required: true
        type: string
      - name: data
        in: body
        description: data to change in storage
        required: true
        schema:
          $ref: "#/definitions/ObjectResponse"
    tags:
      - Change user
    responses:
      "200":
        description: Success
        schema:
          $ref: "#/definitions/ObjectResponse"
      default:
        description: Error
        schema:
          $ref: "#/definitions/ErrorResponse"
